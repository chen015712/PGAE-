'''
Generate structured scientific analysis of Chapter 16 as a Word document.
'''
from docx import Document
from docx.shared import Pt, Inches, Cm, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT
from docx.oxml.ns import qn
import os

doc = Document()

# Style configuration
style = doc.styles['Normal']
font = style.font
font.name = '宋体'
font.size = Pt(11)
style.element.rPr.rFonts.set(qn('w:eastAsia'), '宋体')

# ---- Helper functions ----
def add_heading_styled(text, level):
    h = doc.add_heading(text, level=level)
    for run in h.runs:
        run.font.color.rgb = RGBColor(0, 0, 0)
    return h

def add_para(text, bold=False):
    p = doc.add_paragraph()
    run = p.add_run(text)
    run.bold = bold
    run.font.size = Pt(11)
    run.font.name = '宋体'
    run._element.rPr.rFonts.set(qn('w:eastAsia'), '宋体')
    return p

def add_table_with_data(headers, rows):
    table = doc.add_table(rows=1 + len(rows), cols=len(headers))
    table.style = 'Table Grid'
    table.alignment = WD_TABLE_ALIGNMENT.CENTER
    # Header row
    for j, h in enumerate(headers):
        cell = table.rows[0].cells[j]
        cell.text = h
        for paragraph in cell.paragraphs:
            for run in paragraph.runs:
                run.bold = True
                run.font.size = Pt(9)
                run.font.name = '宋体'
                run._element.rPr.rFonts.set(qn('w:eastAsia'), '宋体')
    # Data rows
    for i, row in enumerate(rows):
        for j, val in enumerate(row):
            cell = table.rows[i+1].cells[j]
            cell.text = str(val)
            for paragraph in cell.paragraphs:
                for run in paragraph.runs:
                    run.font.size = Pt(9)
                    run.font.name = '宋体'
                    run._element.rPr.rFonts.set(qn('w:eastAsia'), '宋体')
    doc.add_paragraph()  # spacing
    return table

# ===========================================================================
# DOCUMENT CONTENT
# ===========================================================================

add_heading_styled('第十六章《实验结果与综合讨论》结构化科学分析', 0)

doc.add_paragraph()

# ===== A. 总体结构概览 =====
add_heading_styled('A. 总体结构概览', 1)

add_para('第十六章作为学位论文的核心实验验证章节，共12节，承担从「模型训练->精度验证->物理一致性->相行为表征->工程应用->鲁棒性->机制解释->总结」的完整论证闭环。各节功能如下：')

overview = [
    ['16.1 引言', '确立验证框架与技术路线'],
    ['16.2-16.3', '基准精度验证（训练->全局泛化）'],
    ['16.4', '物理一致性检验（RR方程、Gibbs判据、K值对等）'],
    ['16.5-16.6', '相行为表征（相边界+相包络）'],
    ['16.7-16.8', '工程可部署性（速度+油藏模拟）'],
    ['16.9', '极端条件下可靠性评估'],
    ['16.10', '潜空间物理语义解释'],
    ['16.11-16.12', '综合讨论->结论'],
]
add_table_with_data(['部分', '功能定位'], overview)

# ===== B. 分部分拆解 =====
add_heading_styled('B. 分部分拆解', 1)

# ---- 16.1 ----
add_heading_styled('B.1 第十六章引言（16.1）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '声明六维验证体系：(1)闪蒸预测精度；(2)热力学一致性；(3)相边界连续性；(4)相包络重构；(5)计算效率；(6)油藏模拟与鲁棒性。明确基准真值来源（WinProp PR-EOS）与数据集规模（5300样本，P∈[100,50000] kPa，T∈[40,180] °C）。'],
        ['逻辑关系', '承接前文章节（模型架构、训练方法设计）->提出问题「该模型在各维度表现如何」->为后续12节的逐项回答提供路线图。'],
        ['段落关键词', 'PGAE, WinProp, PR-EOS, 15组分, 5300样本, 六维验证'],
        ['创新点', '提出将物理约束验证（RR、Gibbs、单调性）与工程应用验证（油藏模拟）置于同一框架下的系统性评估方法。'],
        ['方法亮点', '六维验证框架覆盖「精度->物理->工程->鲁棒性」全链条，每个维度均有独立定量指标，避免单一指标误导。'],
    ]
)

# ---- 16.2 ----
add_heading_styled('B.2 模型训练与验证集性能（16.2）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '报告PGAE在148 epoch达到最佳验证损失0.423后触发早停。验证集关键指标：β MAE=0.053, x MAE=0.0027, y MAE=0.0016, 质量残差=4.08×10⁻⁹, 相分裂误差=0.0031, K值R²=0.943。附潜空间三维可视化(图16-1)和β对等图(图16-2)。'],
        ['逻辑关系', '回应16.1的精度验证目标->为16.3全量级推断提供baseline->K值R²=0.943为16.4.3的K值对等分析铺垫。'],
        ['段落关键词', 'best_epoch=148, val_loss=0.423, β MAE=0.053, 质量残差4.08×10⁻⁹, 早停patience=120, 12项损失函数'],
        ['创新点', '首次以表格形式展示12项损失函数子项在验证集上的精确分解，使读者可追溯每项物理约束的收敛程度。'],
        ['方法亮点', 'P在编码器(17维输入)+8维潜空间+条件单调P网络的三联设计使得K值R²从v2的0.255恢复至0.943。损失函数包含RR(λ_rr=0.1)和K值监督(λ_K=2.0)的显式项，区别于纯数据驱动方法。'],
    ]
)

# ---- 16.3 ----
add_heading_styled('B.3 全局预测精度评估（16.3）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '5300样本全量推断：β MAE=0.044（优于验证集0.053），质量残差=3.53×10⁻⁹，轻组分K MAE=0.043，重组分K MAE=0.063，K值R²=0.966（超越原文献0.947）。'],
        ['逻辑关系', '从验证集(20%)->全数据集(100%)的泛化验证->为16.4的物理一致性分析提供全量级统计基础->K值R²=0.966直接回应「代理模型能否替代EOS」的核心问题。'],
        ['段落关键词', 'K值R²=0.966, β MAE=0.044, 轻组分K MAE=0.043, 重组分K MAE=0.063, 泛化'],
        ['创新点', '报告轻/重组分K值精度差异(0.043 vs 0.063)，并从数据分布(77.4%单相)给予物理层面解释——这是文献中罕见的分组分精度分析。'],
        ['方法亮点', '全量级推断(5300×15组分)提供统计稳健性，避免小样本偏差。分轻重组分的K值分析揭示了模型在不同分子量区间的差异化学表现。'],
    ]
)

# ---- 16.4 ----
add_heading_styled('B.4 热力学一致性分析（16.4）', 2)

add_para('16.4.1 Rachford-Rice方程验证', bold=True)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '全量级RR残差统计：均值4.70×10⁻⁸，中位数3.82×10⁻⁸，p99=1.58×10⁻⁷，|RR|<10⁻⁴通过率100.0%。图16-4展示对数RR分布，所有样本均在log₁₀|RR|<−4区域。'],
        ['逻辑关系', 'RR方程是闪蒸计算的数学定义（f(β)=Σz_i(K_i-1)/(1+β(K_i-1))=0），100%通过率->模型输出是数学定义上严格有效的闪蒸解->为16.5相边界分析和16.8油藏模拟提供合法性基础。'],
        ['段落关键词', 'RR残差, Newton迭代15次, Σz_i(K_i-1)/(1+β(K_i-1))=0, p99=1.58×10⁻⁷, 通过率100%'],
        ['创新点', '解码器中内置Newton-Raphson求解器（15次迭代）实现结构级RR满足，而非后处理校正——这是与Peng-Robinson代理模型的根本区别：传统方法先预测(x,y,β)再校正RR，PGAE则保证RR在生成过程中被精确满足。'],
        ['方法亮点', '采用Huber损失+死区(10⁻⁴)的RR正则化策略：|RR|<10⁻⁴时不施加惩罚，仅对大违反进行平滑惩罚。这一设计避免了过度优化已满足区域造成其他指标的退化。'],
    ]
)

add_para('16.4.2 Gibbs自由能一致性', bold=True)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', 'ΔG/RT均值=−1.20×10⁻⁴，中位数=0.000，违反率34.2%。修复了单相基准态根选择（取min(G_vap, G_liq)替代原先仅用vapour根）。'],
        ['逻辑关系', 'RR方程保证数学一致性，Gibbs检验则回答「预测的相分裂是否热力学自发」这一更深层问题。违反率34.2%的归因分析(PR-EOS近似性、λ_gibbs=0、近临界区数值敏感性)为16.11局限性讨论提供依据。'],
        ['段落关键词', 'ΔG_mix/RT, PR-EOS, 逸度系数, 立方方程求根, λ_gibbs=0.0, 34.2%违反率'],
        ['创新点', '三点归因框架(PR-EOS近似->训练目标偏离->根选择歧义)替代简单的「准确/不准确」二元判断，给出违反率的物理解释。改进的单相参考态选择(vapour和liquid中取min(G))消除了因错误基准态导致的伪违反。'],
        ['方法亮点', 'PR-EOS逸度计算使用30个硬编码参数(Tc, Pc, ω, BIP)，全量级15组分双循环的向量化实现。数值稳定性处理：ln_phi截断至[-20,20]，a_mix采用向量化双求和(避免Python for循环)。'],
    ]
)

add_para('16.4.3 K值对等分析', bold=True)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '图16-6展示log₁₀(K_pred) vs log₁₀(K_true)全量级对等图(15组分×5300样本)。K值跨越~10个数量级(10⁻⁴至10⁶)，R²=0.966。在K≈1(单相)区域密度最高、精度最高；在K极值区域数据稀疏。'],
        ['逻辑关系', 'K值是连接微观(分子间相互作用)与宏观(相行为)的桥梁变量，其精度直接决定β、x、y、相包络等一系列下游预测的可靠性。K值R²=0.966->相包络重构(16.6)可行。'],
        ['段落关键词', 'K_i=y_i/x_i, log₁₀空间, 二维hexbin密度图, 10个数量级, 77.4%单相'],
        ['创新点', '在log₁₀空间而非线性空间进行K值对等分析——因为K值跨多个数量级，log空间使全量级偏差可视化和定量评估同等重要。hexbin密度图替代散点图解决了大量重叠点导致的视觉误导。'],
        ['方法亮点', '使用K_pred(来自模型解码器)而非y_pred/x_pred(有效K)进行对等分析，分离K值学习与β求解的耦合关系。'],
    ]
)

# ---- 16.5 ----
add_heading_styled('B.5 相边界连续性验证（16.5）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '在3种组成(典型/Oil-rich/Gas-rich)×多个温度下的500点P扫描。T=80°C时β范围[0.015,0.962]捕获完整过渡，但点对点单调性违反252/499(50.5%)（最大跳跃仅0.017）。随T升高->β范围收窄->β MAE改善(120°C时0.032)。'],
        ['逻辑关系', 'P扫描的β-P曲线是相包络(16.6)的微观基础——每一固定T下的β-P扫描回答「在该温度下压力如何驱动相变」。50.5%的微观单调性违反vs仅2.8%的宏观违反(相包络级别)->揭示评估尺度的重要性。'],
        ['段落关键词', '500点P扫描, β-P曲线, 点对点单调性, max_jump=0.017, β MAE=0.032(120°C), 气泡点/露点二分搜索'],
        ['创新点', '区分「微观单调性违反」(点对点Δβ/ΔP>0)与「宏观单调性违反」(气泡点/露点位置错误)。指出50.5%的微观违反率主要来自局部数值波动(而非物理错误)，可通过平滑滤波消除，不影响宏观相边界判断。'],
        ['方法亮点', '500点超高分辨率P扫描(而非文献中常见的20-50点)提供亚kPa级别的相边界检测精度。配合WinProp EOS 15点网格基准形成「高密度代理+稀疏基准」的高效验证策略。'],
    ]
)

# ---- 16.6 ----
add_heading_styled('B.6 相包络重构能力（16.6）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '3种组成的P-T相包络重构：Gas-rich CH₄=62.8%的T_crit偏差3.6°C(相对误差1.98%)、P_crit偏差1077 kPa(4.3%)。Typical T_crit偏差33.3°C,P_crit偏差17 kPa(0.086%)。Oil-rich T_crit偏差63.9°C,P_crit偏差297 kPa(1.68%)。附β(P,T)热力图和潜空间相包络轨迹。'],
        ['逻辑关系', '相包络是PGAE模型能力的「终极测试」——综合考察β-P扫描(16.5)、K值精度(16.4.3)、RR一致性(16.4.1)的协同效果。T_crit与CH₄含量的关系(Gas-rich最优->Oil-rich最差)递推至16.3重组分K值偏差更大的发现。'],
        ['段落关键词', 'P-T相包络, 气泡曲线, 露点曲线, 临界点, T_crit偏差3.6-63.9°C, P_crit偏差17-1077 kPa, 改进的饱和压力提取算法'],
        ['创新点', '改进的饱和压力提取算法(四策略级联+三策略临界点外推)使饱和点检测率达到100%(50/50 T点)，解决了原始算法在高温区完全失效的问题。P_crit在所有组成上均极其精确(偏差17-1077 kPa)。Gas-rich的T_crit仅3.6°C偏差证明了模型在天然气型流体上的实用价值。'],
        ['方法亮点', '四策略检测链：(1)直接阈值穿越->(2)梯度拐点(dβ/dlogP)->(3)两相区边界->(4)max-β伪点。三策略临界外推：(1)幂律间隙(ΔP∝(T_c-T)^β)->(2)二次拟合->(3)最近逼近。P_min=10 kPa足够检测低压露点,P_max=50000 kPa覆盖高压气泡点。'],
    ]
)

# ---- 16.7 ----
add_heading_styled('B.7 计算效率对比（16.7）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '单次闪蒸：PGAE 12.8 ms vs WinProp 318.3 ms(24.9×加速)。批量吞吐率68,470 samples/s(bs=16384)。模型118,494参数,0.45 MB。CPU推理,无GPU加速。'],
        ['逻辑关系', '计算效率是代理模型的工程存在理由——若速度无优势则无替代EOS的必要。24.9×加速(单样本)和68,470/s(批量)为16.8油藏模拟(数万次闪蒸调用)提供可行性前提。'],
        ['段落关键词', '24.9×加速, 68,470 samples/s, bs=16384, 118,494参数, 0.45 MB, CPU推理'],
        ['创新点', '报告从batch_size=1至16384的完整吞吐率扩展曲线，揭示PAE从延迟敏感(单样本12.8ms)到吞吐敏感(16384批量≈240ms处理)的双模式运行能力。'],
        ['方法亮点', '单样本15次重复计时(报告mean/median/std/min/max/p99)，批量吞吐率覆盖11个batch_size(1->16384)，WinProp基准亦为15次重复。计时排除模型加载与数据预处理，仅测量纯推理时间。'],
    ]
)

# ---- 16.8 ----
add_heading_styled('B.8 油藏模拟应用验证（16.8）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '四个模拟场景：CVD衰竭(100次闪蒸,β MAE=0.119)、CO₂注入(3040次,β MAE=0.014)、五点井网(18963次,β MAE=0.00015)、径向近井(70876次)。模拟时间1.56-311.98秒。'],
        ['逻辑关系', '16.6回答了「单点精度」，16.8回答「序列/空间耦合下的累积精度」——这是代理模型在真实油藏模拟器中部署的最终考验。五点井网β MAE=0.00015(接近零误差)->模型的累积误差不随闪蒸调用次数增长而放大。'],
        ['段落关键词', 'CVD, CO₂注入, 五点井网, 径向近井, β MAE=0.00015(五点井网), 闪蒸速率59-227/s, WinProp缓存命中率597/646'],
        ['创新点', '在四个物理场景(衰竭、注入、井网、近井)上统一验证，覆盖油藏工程的主要操作类型。CO₂注入场景准确捕获前缘推进中的相态变化。CVD场景揭示模型在极端压力跨度下的误差特征。'],
        ['方法亮点', 'WinProp缓存策略(命中率92.4%)大幅减少重复EOS调用。PGAE-WinProp混合验证模式：PGAE执行全量闪蒸->稀疏抽取验证点调用WinProp->MAE/RMSE量化。此模式在保证统计显著性的同时控制WinProp计算开销。'],
    ]
)

# ---- 16.9 ----
add_heading_styled('B.9 鲁棒性与可靠性评估（16.9）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '五个子测试：(1)噪声敏感性：27条件×500 MC，两相区高噪下β std≤0.032，零次物理限界违反。(2)外推：P/T扫描超出训练域，β始终∈[0,1]。(3)Lipschitz：L_mean=0.5-7.9,L_max=64.5(相边界区)。(4)约束应力：36个极端条件，100%通过率。(5)对抗搜索：max Δβ=1.0,mean Δβ=0.549。'],
        ['逻辑关系', '16.2-16.8验证了模型在「正常条件」下的表现，16.9检验在「异常/极端条件」下是否崩溃——这是工程部署的安全边界测试。100%约束通过率证明模型的内置归一化和P网络约束是有效的防崩溃机制。'],
        ['段落关键词', '500次MC, β std≤0.032, 零限界违反, Lipschitz L_max=64.5, 100%约束通过率, 对抗搜索Δβ_mean=0.549'],
        ['创新点', '发现相边界区域L_max=64.5(而非模型缺陷)->给出物理归因：相边界附近微小的组成变化即可触发β从0->1，高Lipschitz常数是物理真实的「相变敏感性」。纯组分和极端P/T下的100%约束通过率证明归一化层的鲁棒性。'],
        ['方法亮点', '噪声敏感性采用3组成×3相态×3噪声水平×500 MC的全因子设计(27×500=13500次推断)。Lipschitz常数在z空间近邻对(Δz=0.01)上估计。对抗搜索在历史敏感区域加扰动寻找最大Δβ。每个子测试设计独立、互补、可复现。'],
    ]
)

# ---- 16.10 ----
add_heading_styled('B.10 潜空间物理特征分析（16.10）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '8维潜空间的PCA三维投影(图16-24)->β值沿主方向有序排列；UMAP二维相态聚类(图16-25)->液相/气相形成分离簇、两相区为连续过渡带；物理量相关性热力图(图16-26)->特定潜维度与特定物理量(P,T,β,K_CH₄,K_C10+)有显著相关性；沿主方向β连续插值(图16-27)。'],
        ['逻辑关系', '潜空间是PGAE的「黑箱内部」——16.10的研究是模型可解释性分析，回答「模型是否学习到物理有意义的表征」->为16.11的机制讨论提供几何层面的证据。'],
        ['段落关键词', '8维潜空间, PCA, UMAP, 相关性热力图, 插值平滑性, 相态聚类, 物理表征'],
        ['创新点', '证明潜维度与物理量(如K_CH₄,K_C10+)之间存在可量化的相关性，打破了深度学习模型「不可解释」的常见批评。潜空间中液相->两相->气相的连续过渡结构与经典相变理论的序参量描述一致。'],
        ['方法亮点', '综合使用PCA(线性降维)+UMAP(非线性流形学习)两种互补的可视化方法，从线性和非线性两个角度揭示潜空间结构。相关性分析使用Pearson系数(线性)和Spearman系数(单调)双重度量。'],
    ]
)

# ---- 16.11 ----
add_heading_styled('B.11 综合讨论（16.11）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '三大板块：(1)主要成果：7点定量总结(K R²=0.966, β MAE=0.044, RR 100%, 单调违反2.8%, T_crit偏差3.6°C, 25×加速, 五点井网β MAE=0.00015)。(2)局限性：4点分析(相包络T_crit, Gibbs 34.2%, 重组分K值, 单相区K值)。(3)与现有工作对比：4个差异化优势(物理内置, 结构单调性, 端到端, 可解释潜空间)。'],
        ['逻辑关系', '16.11是16.2-16.10的提炼升华——「成果」从各节选取最具代表性的定量指标，「局限」对应各节中暴露的系统性不足，「对比」定位PGAE在文献中的独特位置。'],
        ['段落关键词', '7项成果, 4项局限, 4项差异化优势, 结构单调性, 物理内置vs后验约束'],
        ['创新点', '提出「物理内置(physics-built-in)vs后验约束(post-hoc constraint)」的代理模型分类框架，将PGAE归为前者。指出Gibbs违反率34.2%的多层归因(PR-EOS近似->训练目标偏离->数值敏感性)而非简单视为模型缺陷。'],
        ['方法亮点', '局限性分析为每个问题给出具体数值(63.9°C, 34.2%, MAE 0.063 vs 0.043)和可操作的改进方向(λ_gibbs启用、高T数据增强)，使讨论具有工程指导价值。'],
    ]
)

# ---- 16.12 ----
add_heading_styled('B.12 本章小结（16.12）', 2)

add_table_with_data(
    ['维度', '内容'],
    [
        ['核心内容', '6条结论对应本章6个验证维度：(1)K R²=0.966,β MAE=0.044；(2)RR 100%,单调违反2.8%；(3)Gas-rich T_crit偏差3.6°C；(4)25×加速,0.45 MB；(5)五点井网MAE=0.00015；(6)100%约束率。'],
        ['逻辑关系', '将12节的详细分析压缩为6条可独立引用的定量结论。每条结论对应前文的特定小节，可被「结论与展望」（第十七章）直接引用。'],
        ['段落关键词', '6条结论, K R²=0.966, RR=100%, T_crit=3.6°C, 25×加速, β MAE=0.00015, 100%约束率'],
        ['创新点', '采用「每维度一句结论+具体数值」的标准化格式，使章节结论可直接作为论文摘要和答辩PPT的素材。'],
        ['方法亮点', '结论的每条陈述均可追溯到前文的具体小节和表格，形成「结论<-证据<-原始数据」的完整溯源链。'],
    ]
)

# ===== C. 结构联动说明 =====
add_heading_styled('C. 结构联动说明：从论证链到证据链', 1)

add_para('第十六章以「闪蒸代理模型的系统验证」为核心论题，形成以下完整论证链路：')

add_para('层次1——精度奠基（16.2->16.3）', bold=True)
add_para('训练收敛(16.2)证明了模型设计(前文架构章节)的可行性->全量推断(16.3)证明了泛化能力。K值R²从验证集0.943提升至全量级0.966的现象说明验证集的分割未引入偏差。')

add_para('层次2——物理保证（16.4）', bold=True)
add_para('RR 100%通过率(16.4.1)->模型输出的每一组(β,x,y)在数学定义上是有效的闪蒸解。Gibbs分析(16.4.2)->在PR-EOS框架下检验热力学自发性，34.2%违反率不是表面数字而需三层归因(PR-EOS近似+训练目标+数值敏感)。K值对等(16.4.3)->R²=0.966证明模型学习了正确的微观K值而非仅表面拟合。')

add_para('层次3——宏观相行为（16.5->16.6）', bold=True)
add_para('β-P微观扫描(16.5)提供相变的「一维切片」->相包络(16.6)将这些切片组装为完整的P-T相图。Gas-rich T_crit偏差3.6°C证明当组成在数据密集区时模型能力充分->Oil-rich偏差63.9°C暴露数据稀疏区的不足->这一对比本身就构成了「模型能力与数据分布关系」的科学证据。')

add_para('层次4——工程部署（16.7->16.8）', bold=True)
add_para('25×加速(16.7)提供了部署的经济理由->油藏模拟(16.8)证明了在真实工程场景中精度不因累积而衰减。五点井网MAE=0.00015是层次1-3所有优势的合力结果：精确的K值(16.4.3)+物理一致的RR(16.4.1)+正确的相边界(16.5)->空间分布正确的β场。')

add_para('层次5——边界条件（16.9）', bold=True)
add_para('异常条件的鲁棒性测试定义了模型的「安全运行域」。100%约束通过率->模型在任何输入下都不会输出β∉[0,1]或Σx≠1的无意义结果。Lipschitz分析->识别相边界为高敏感区(物理真实)而非模型不稳定点。')

add_para('层次6——机制解释（16.10）', bold=True)
add_para('潜空间分析将「模型是否有效」的问题推进到「模型为什么有效」。潜维度与K_CH₄、K_C10+的相关性->编码器自组织地学习了组分K值的区别化表征，而非简单记忆训练数据。')

add_para('层次7——提炼升华（16.11->16.12）', bold=True)
add_para('综合讨论将层次1-6的证据提炼为4项差异化优势(物理内置、结构单调性、端到端、可解释)+4项可操作的局限性。小结将全文压缩为6条定量结论，形成向第十七章的接口。')

add_para('∴ 论证链 = 精度基线 -> 物理约束 -> 宏观验证 -> 工程部署 -> 鲁棒边界 -> 机制解释 -> 结论', bold=True)

doc.add_paragraph()

# ---- Missing content annotation ----
add_heading_styled('D. 信息完整性标注', 1)

missing_items = [
    ['16.2 训练损失曲线', '在提供的文本中未找到训练过程损失曲线图（通常为epoch-loss plot）。图16-1为潜空间散点图而非损失曲线。'],
    ['16.4.2 Gibbs修复效果', 'Gibbs违反率在修复前后未见显著差异（修复前34.21%，修复后34.21%），原因为大多数样本G_vap<G_liq，min操作不改变结果。这已在文中说明为PR-EOS固有限制。'],
    ['16.6 相包络与WinProp逐点对比', '提供了临界点偏差和对等图，但缺少沿温度网格的逐点P_bubble/P_dew插值误差曲线。bubble_P_mae和dew_P_mae提供了汇总统计。'],
    ['16.8 径向近井场景β MAE', '数据文件被截断（数据量过大），文中未报告该场景的β MAE值，仅报告了模拟时间和闪蒸速率。'],
    ['16.10 潜空间分析详细方法', '潜空间分析图来自latent_analysis.py脚本，但本章未详细描述PCA/UMAP的具体参数设置和计算流程。'],
]
add_table_with_data(['位置', '说明'], missing_items)

# ===== E. 关键证据-主张对应表 =====
add_heading_styled('E. 关键证据-主张对应表', 1)

evidence_claims = [
    ['K值R²=0.966', '图16-6, 表16-2', 'PGAE精确学习了K值', '强（全量级5300样本，R²>0.95）'],
    ['RR 100%通过', '图16-4, 表16-3', '模型输出是数学有效的闪蒸解', '强（5300样本无一违反）'],
    ['Gas-rich T_crit 3.6°C', '图16-9, 表16-6', '模型可重建天然气相包络', '中等（1种组成，3个测试案例）'],
    ['五点井网β MAE=0.00015', '表16-9', '模型适用于生产模拟', '强（18963次调用，误差<0.1%）'],
    ['25×加速', '表16-7, 图16-18', '模型具有工程部署价值', '强（15次重复计时，双模式）'],
    ['100%约束通过', '表16-5, 16.9.4节', '模型在任何输入下都不崩溃', '强（36个极端条件×多种组成）'],
    ['β-P单调性2.8%违反', '表16-5, 16.5.1节', '模型满足物理单调性', '中等（微观50.5%vs宏观2.8%，需区分）'],
]
add_table_with_data(['主张', '证据来源', '含义', '证据强度'], evidence_claims)

doc.add_paragraph()

add_para('注：本文档严格基于第十六章文本内容和outputs/目录下的实验数据文件编写。所有定量数值均可从对应JSON/CSV文件中溯源。[证据强度]的评估基于样本量、重复次数、对照组完备性和效应大小四个维度综合判定。', bold=False)

# ===== SAVE =====
output_path = r'C:\Users\Lenovo\Desktop\fluid_15comp_file\outputs\第十六章_结构化科学分析.docx'
doc.save(output_path)
print(f'Word document saved to: {output_path}')
